/**
 * src/utils/AppError.js
 * ------------------------------------------------------------
 * Owner : Person A — Foundation & Auth
 * Layer : util
 *
 * Responsibility:
 *   Custom error class so you can throw new AppError('Job not found', 404) anywhere and have error.middleware.js catch it consistently.
 *
 * Build this file to:
 *   - class AppError extends Error
 *   - constructor(message, statusCode) -> super(message); this.statusCode = statusCode; this.isOperational = true;
 *
 * Reference: Recruiting_System_Backend_Plan.docx -> Section 7 (Stub Your Utils, Helpers & File Uploads)
 */

// TODO: implement
