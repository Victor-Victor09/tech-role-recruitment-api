/**
 * src/utils/token.js
 * ------------------------------------------------------------
 * Owner : Person A — Foundation & Auth
 * Layer : util
 *
 * Responsibility:
 *   Thin wrappers around jsonwebtoken.
 *
 * Build this file to:
 *   - signToken(payload) -> jwt.sign(payload, config.jwtSecret, { expiresIn: config.jwtExpiresIn })
 *   - verifyToken(token) -> jwt.verify(token, config.jwtSecret)
 *
 * Depends on / imports from:
 *   - jsonwebtoken
 *   - src/config/env.js
 *
 * Reference: Recruiting_System_Backend_Plan.docx -> Section 7 (Stub Your Utils, Helpers & File Uploads)
 */

// TODO: implement
