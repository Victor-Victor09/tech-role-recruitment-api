/**
 * src/utils/catchAsync.js
 * ------------------------------------------------------------
 * Owner : Person A — Foundation & Auth
 * Layer : util
 *
 * Responsibility:
 *   Wraps an async controller/middleware function so you never write try/catch in a controller.
 *
 * Build this file to:
 *   - module.exports = (fn) => (req, res, next) => fn(req, res, next).catch(next);
 *
 * Reference: Recruiting_System_Backend_Plan.docx -> Section 7 (Stub Your Utils, Helpers & File Uploads)
 */

// TODO: implement
