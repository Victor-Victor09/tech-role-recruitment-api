/**
 * src/services/jobListing.service.js
 * ------------------------------------------------------------
 * Owner : Person C — Employer Track
 * Layer : service (business logic — the CRUD "brain")
 *
 * Responsibility:
 *   This is where validation results are used, DB calls happen (via the
 *   matching model file), and business rules live. Controllers call these
 *   functions and do nothing else.
 *
 * Build this file to:
 *   - create(employerId, data)
 *   - update(id, employerId, data) -> reject if the listing doesn't belong to this employer
 *   - remove(id, employerId) -> soft delete (status = 'closed'), not a DB delete
 *   - list(filters) -> search/filter by tech stack, work preference, location
 *
 * Depends on / imports from:
 *   - src/models/jobListing.model.js
 *   - src/utils/AppError.js
 *
 * Reference: Recruiting_System_Backend_Plan.docx -> Section 8 (CRUD Services)
 */

// TODO: implement
